<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AideDeJeu.Views.AboutPage"
             xmlns:vm="clr-namespace:AideDeJeu.ViewModels;"
             Title="{Binding Title}" 
             IsBusy="{Binding IsBusy}">
    <ContentPage.BindingContext>
        <vm:AboutViewModel />
    </ContentPage.BindingContext>
    <!--<ContentPage.ToolbarItems>
        <ToolbarItem Text="truc" />
    </ContentPage.ToolbarItems>-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackLayout BackgroundColor="Black" VerticalOptions="FillAndExpand" HorizontalOptions="Fill">
            <StackLayout BackgroundColor="Black" Orientation="Horizontal" HorizontalOptions="Center" VerticalOptions="Center">
                <ContentView BackgroundColor="Black" Padding="20,20,20,20" VerticalOptions="FillAndExpand">
                    <Image BackgroundColor="Black" Source="black_book.png" VerticalOptions="Center" HeightRequest="64" />
                </ContentView>
            </StackLayout>
        </StackLayout>
        <StackLayout Grid.Column="1" Grid.Row="0" BackgroundColor="Black" VerticalOptions="FillAndExpand" HorizontalOptions="Fill">
            <StackLayout BackgroundColor="Black" Orientation="Vertical" HorizontalOptions="Center" VerticalOptions="Center">
                <Label Text="Aide de Jeu " FontSize="32" FontAttributes="Bold" TextColor="{StaticResource bgtan}" />
                <Label Text="{Binding Version, StringFormat='Version {0}'}" TextColor="{StaticResource bgtan}" />
            </StackLayout>
        </StackLayout>
        <ScrollView Grid.Row="1" Grid.ColumnSpan="2">
            <StackLayout Orientation="Vertical" Padding="16,40,16,40" Spacing="10">
                <Label Text="Cette application est une aide de jeu pour le plus célèbre des jeux de rôle" />
                <Label Text="Son contenu provient du site AideDD, et est traduit du SRD" />
                <Label Text="Il est soumis à la licence OGL" />
                <Label>
                    <Label.FormattedText>
                        <FormattedString>
                            <Span Text="Les icones proviennent du site " />
                            <Span Text="Game-icons.net" FontAttributes="Bold" />
                        </FormattedString>
                    </Label.FormattedText>
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding OpenWebCommand}" CommandParameter="http://game-icons.net" />
                    </Label.GestureRecognizers>
                </Label>

                
                <Button Margin="0,10,0,0" Text="Site web" Command="{Binding OpenWebCommand}" CommandParameter="https://nioux.github.io/AideDeJeu/" BackgroundColor="{StaticResource titlered}" TextColor="{StaticResource bgtan}" />


            </StackLayout>
        </ScrollView>
    </Grid>
</ContentPage>